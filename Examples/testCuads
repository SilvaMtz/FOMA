program Main
start
 int a, b, c, d, e, f;

  a = b ;
  c = d + e * f;
  g = d and x < h;

  if (x < z)
  start
    x = x + y;
  end
  else
  start
    z = z + 25;
  end

  while (a < b)
  start
    y = p + m;
  end


  for (x = 5 ; x < z ; x = x + 1)
  start
    v = d;
  end

  print(a);
  print(c, d);
  print(e + f * g);

  input(x);
  input(y);

  x = 10;

end
